# Website Design Recreation

This project recreates website designs from a reference screenshot into a single `index.html` using Tailwind CSS, iterating against Puppeteer screenshots until the result matches closely.

See `.claude/rules/` for the full workflow, technical defaults, and matching rules.
